{
  "name": "potluck-poc",
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "proxy": "node proxy.mjs"
  },
  "dependencies": {
    "cors-anywhere": "^0.4.4"
  },
  "devDependencies": {
    "vitest": "^4.1.0"
  }
}
